import type { IGroupVideoConference, IVideoConference } from '../videoConferences/IVideoConference';
export declare type VideoConfData = Pick<IVideoConference, '_id' | 'type' | 'rid' | 'createdBy' | 'providerData'> & {
    title?: IGroupVideoConference['title'];
};
export declare type VideoConfDataExtended = VideoConfData & Required<Pick<IVideoConference, 'url'>>;
