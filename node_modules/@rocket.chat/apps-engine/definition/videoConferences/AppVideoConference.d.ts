import type { IGroupVideoConference } from './IVideoConference';
export declare type AppVideoConference = Pick<IGroupVideoConference, 'rid' | 'providerName' | 'providerData' | 'title'> & {
    createdBy: IGroupVideoConference['createdBy']['_id'];
};
